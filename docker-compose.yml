services:
  api:
    build: .
    ports:
      - "8001:8000"
    environment:
      - DB_PATH=data/csa_planner.db
    volumes:
      - csa_data:/app/data

  frontend:
    build:
      context: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - api

volumes:
  csa_data:
